# Types Layer (TypeScript Only)

## Purpose

Central place for TypeScript interfaces and types.

Examples:

* Device interface
* Telemetry interface
* API response types
* Auth user type

## Rules

* Keep types reusable.
* Avoid duplicating types.
* Use consistent naming.

## Benefit

Improves maintainability and prevents runtime errors.
