# Custom Hooks Layer

## Purpose

Reusable React logic extracted from components.

Examples:

* useAuth
* useDevices
* useTelemetry
* useDebounce

## Rules

* Hooks must start with "use"
* Keep logic reusable
* Avoid UI code inside hooks

## Benefit

Prevents duplicate logic across components.
